<project xmlns="http://maven.apache.org/POM/4.0.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <groupId>de.fhdo.lemma.model_processing.code_generation.springcloud.eureka</groupId>
    <artifactId>de.fhdo.lemma.model_processing.code_generation.springcloud.eureka</artifactId>
    <version>0.8.6-SNAPSHOT</version>
    <name>Eureka Code Generator</name>
    <description>A LEMMA code generator for Eureka components.</description>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <java.version>17</java.version>
        <maven.compiler.plugin.version>3.11.0</maven.compiler.plugin.version>
        <xtend.version>2.32.0</xtend.version>
        <snakeyaml.version>1.21</snakeyaml.version>
        <maven-assembly.version>3.6.0</maven-assembly.version>
    </properties>

    <dependencies>
        <dependency>
            <groupId>de.fhdo.lemma.model_processing.code_generation.deployment_base</groupId>
            <artifactId>de.fhdo.lemma.model_processing.code_generation.deployment_base</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>org.eclipse.xtend</groupId>
            <artifactId>org.eclipse.xtend.lib</artifactId>
            <version>${xtend.version}</version>
        </dependency>
        <dependency>
            <groupId>de.fhdo.lemma.model_processing</groupId>
            <artifactId>de.fhdo.lemma.model_processing</artifactId>
            <version>${project.version}</version>
            <classifier>all-dependencies</classifier>
        </dependency>
        <dependency>
            <groupId>de.fhdo.lemma.operationdsl</groupId>
            <artifactId>de.fhdo.lemma.operationdsl</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>de.fhdo.lemma.data.datadsl</groupId>
            <artifactId>de.fhdo.lemma.data.datadsl.metamodel</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>de.fhdo.lemma.servicedsl</groupId>
            <artifactId>de.fhdo.lemma.servicedsl.metamodel</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>de.fhdo.lemma.technology.technologydsl</groupId>
            <artifactId>de.fhdo.lemma.technology.technologydsl.metamodel</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>de.fhdo.lemma.servicedsl</groupId>
            <artifactId>de.fhdo.lemma.servicedsl</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>de.fhdo.lemma.data.datadsl</groupId>
            <artifactId>de.fhdo.lemma.data.datadsl</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>de.fhdo.lemma.technology.technologydsl</groupId>
            <artifactId>de.fhdo.lemma.technology.technologydsl</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>de.fhdo.lemma.intermediate</groupId>
            <artifactId>de.fhdo.lemma.operation.intermediate.metamodel</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>de.fhdo.lemma</groupId>
            <artifactId>de.fhdo.lemma.utils</artifactId>
            <version>${project.version}</version>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.eclipse.xtend</groupId>
                <artifactId>xtend-maven-plugin</artifactId>
                <version>${xtend.version}</version>
                <executions>
                    <execution>
                        <goals>
                            <goal>compile</goal>
                            <goal>testCompile</goal>
                        </goals>
                        <configuration>
                            <outputDirectory>${project.build.directory}/xtend-gen/main</outputDirectory>
                            <testOutputDirectory>${project.build.directory}/xtend-gen/test</testOutputDirectory>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>${maven.compiler.plugin.version}</version>
                <configuration>
                    <source>${java.version}</source>
                    <target>${java.version}</target>
                </configuration>
            </plugin>

            <plugin>
              <artifactId>maven-assembly-plugin</artifactId>
              <version>${maven-assembly.version}</version>
              <configuration>
               <appendAssemblyId>false</appendAssemblyId>
                <finalName>${project.artifactId}-${project.version}-standalone</finalName>
                <archive>
                  <manifest>
                    <mainClass>de.fhdo.lemma.model_processing.code_generation.springcloud.eureka.EurekaGenerator</mainClass>
                  </manifest>
                  <manifestEntries>
                    <!-- Prevent "WARNING: sun.reflect.Reflection.getCallerClass is not supported" from log4j -->
                    <Multi-Release>true</Multi-Release>
                  </manifestEntries>
                </archive>
                <descriptors>
                    <descriptor>assembly.xml</descriptor>
                </descriptors>
              </configuration>
              <executions>
                <execution>
                  <id>make-assembly</id> <!-- this is used for inheritance merges -->
                  <phase>package</phase> <!-- bind to the packaging phase -->
                  <goals>
                    <goal>single</goal>
                  </goals>
                </execution>
              </executions>
            </plugin>
          </plugins>
    </build>
</project>